---
galaxy_info:
  author: haspadar
  description: >-
    nginx server layer: main config, directory ownership, log rotation,
    optional basic auth, and the settings nginx accepts once per machine —
    Cloudflare real-IP refreshed by a timer and a 444 default server. Per-site
    vhosts and log formats belong to each network's site generator.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - nginx
    - web

dependencies: []
