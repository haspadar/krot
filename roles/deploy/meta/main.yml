---
galaxy_info:
  author: haspadar
  description: >-
    Runs the project's own Deployer from the control machine, so provisioning and
    rollout share one entry point. Releases, symlinks and rollback stay in
    deploy.php — this role does not reimplement them.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - deployment

dependencies: []
