# Tasks: CI ждёт дольше, чем работает

## Замер (`gh run list`, 2026-08-15, окно 2026-07-30 … 2026-08-15)

- [x] **krot публичный**: `gh api repos/haspadar/krot` -> `private: false`. Минуты Actions
      для публичных репозиториев бесплатны — исходная посылка change про расход квоты
      **опровергнута**. Приватны busel и matilda, там экономия и уместна
- [x] 65 прогонов за 17 дней, 64 success / 1 failure
- [x] Длительность: min 37s, **медиана 49s**, max 120s; суммарно 3419s
- [x] По событию: `pull_request` 43, `push` 22
- [x] **Повторных прогонов 20, но реально пересеклись по времени только 2.** Посчитано по
      `startedAt`/`updatedAt` внутри каждой ветки: `concurrency` отменила бы **58 секунд из
      3419 (1,7%)**, а не 31%, как стояло в первой редакции. Разрывы между пушами — сотни
      секунд, дважды больше суток; при медиане 49s отменять нечего
- [x] Разбивка шагов последнего прогона: `Install linters` **8s**, `Install collections`
      **8s**, `ansible-lint` 15s, остальное 0–1s. **16s из 34s — установка зависимостей**.
      Сумма шагов job 34s, wall clock прогона 39s — метрики разные, медиана 49s считается
      по второй
- [x] Разброс есть: на соседнем прогоне `Install collections` занял 16s вместо 8s
- [x] **Защита main крепче, чем утверждала первая редакция**:
      `gh api repos/haspadar/krot/branches/main/protection` -> `strict: true`,
      `enforce_admins: true`, `allow_force_pushes: false`, `allow_deletions: false`,
      контексты `["lint"]`. Прямого пути в main нет ни у кого, включая владельца
- [x] busel для сверки: `protected: false`; `push: [main]` там снят в `b04867e` и
      **возвращён** в `6cac47c` именно из-за отсутствия защиты — переносить его текущий
      workflow в krot вслепую нельзя
- [x] `paths-ignore` из busel не переносится: CI krot читает `wiki/` (`wiki-lint.py` +
      сверка `wiki/index/`), а страницы вики — это `.md`

## Правка
- [ ] `cache: pip` в `actions/setup-python`
- [ ] Кеш каталога коллекций с ключом по хешу `requirements.yml`
- [ ] `concurrency` с группой по `ref` и `cancel-in-progress: true`
- [ ] Комментарий в workflow: `concurrency` заведена авансом под Molecule, её сегодняшний
      вклад 1,7% — чтобы следующий читатель не принял её за меру экономии
- [ ] `.openspec.yaml` с `skip_specs: true`
- [ ] `CHANGELOG.md` — решить, как версионировать правку, не меняющую роли

## Проверка
- [ ] Кеш срабатывает на втором прогоне: шаги установки быстрее 8s
- [ ] Смена `requirements.yml` **инвалидирует** кеш коллекций — иначе протухший кеш даст
      зелёный CI на сломанной зависимости
- [ ] Второй пуш в ветку **отменяет** незавершённый прогон — проверить по факту, а не по
      конфигу: группа по SHA не отменяет ничего, и ошибка выглядит как работающая настройка
- [ ] Правка только `wiki/` по-прежнему гоняет `wiki-lint` (проверка, что `paths-ignore`
      не просочился)
- [ ] `push: [main]` на месте и прогон после мержа происходит

## Не делать здесь
- Снятие `push: [main]` — рассмотрено и отклонено: экономить нечего, а прямая запись
  «состояние main проверено» имеет свою ценность
- `paths-ignore` — риск отключить проверку вики
- gitleaks — в krot его нет, заводить сканер секретов отдельным решением
- Менять настройки защиты main — проверены и достаточны
