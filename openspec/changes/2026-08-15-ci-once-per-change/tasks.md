# Tasks: CI платит за ответы, которые уже даны

## Замер (`gh run list`, 2026-08-15, окно 2026-07-30 … 2026-08-15)

- [x] 65 прогонов за 17 дней, 64 success / 1 failure
- [x] Длительность: min 37s, **медиана 49s**, max 120s; суммарно 3419s ≈ 57 минут
- [x] По событию: `pull_request` 43, **`push` 22** (34% расхода на повторную проверку)
- [x] 23 PR-ветки на 43 прогона; у 11 веток >1 прогона; **20 прогонов вытеснила бы
      `concurrency`** (31%). Лидеры по 4: `feat/llm-wiki`, `docs/openspec`, `chore/pg18`,
      `infra/deploy`
- [x] Разбивка шагов последнего прогона: `Install linters` **8s**, `Install collections`
      **8s**, `ansible-lint` 15s, остальное 0–1s. **16s из 34s — установка зависимостей**
- [x] **`main` защищён**: `gh api repos/haspadar/krot/branches/main` -> `protected: true`,
      required checks `["lint"]`. Это условие, при котором снятие `push: [main]` безопасно
- [x] Проверено расхождение с busel: там `protected: false`, `push: [main]` был снят и
      **возвращён** (`6cac47c`) — копировать его текущий workflow вслепую нельзя
- [x] Проверено, что `paths-ignore` из busel не переносится: CI krot читает `wiki/`
      (`wiki-lint.py` + сверка `wiki/index/`), а страницы вики — это `.md`

## Правка
- [ ] `concurrency` с группой по `ref` и `cancel-in-progress: true`
- [ ] Убрать `push: [main]`, добавить `workflow_dispatch`
- [ ] Комментарий в workflow: экономия держится на защите main — сняв защиту, вернуть прогон
- [ ] `cache: pip` в `actions/setup-python`
- [ ] Кеш каталога коллекций с ключом по хешу `requirements.yml`
- [ ] `CHANGELOG.md`

## Проверка
- [ ] Второй пуш в ветку **отменяет** первый прогон — проверить по факту, а не по конфигу:
      группа по SHA не отменяет ничего, и ошибка выглядит как работающая настройка
- [ ] Мерж PR **не** порождает прогон
- [ ] `workflow_dispatch` запускается руками и проходит
- [ ] Кеш срабатывает на втором прогоне: шаги установки быстрее 8s
- [ ] Смена `requirements.yml` **инвалидирует** кеш коллекций — иначе протухший кеш даст
      зелёный CI на сломанной зависимости
- [ ] Правка только `wiki/` по-прежнему гоняет `wiki-lint` (проверка, что `paths-ignore`
      не просочился)
- [ ] Замерен расход после правки и сверен с ожиданием ≈ вдвое

## Не делать здесь
- [ ] `paths-ignore` — обоснование в proposal, экономия мала, риск отключить проверку вики
- [ ] gitleaks — в krot его нет, заводить сканер секретов отдельным решением
- [ ] Менять настройки защиты main
