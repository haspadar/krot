---
# Collections Krot's own roles depend on. Install with:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix
    version: ">=1.5.4"
  - name: community.general
    version: ">=9.0.0"
  - name: community.postgresql
    version: ">=3.4.0"
